export function allocateRawCode(code, highWaterMark, ledger) {
  if (code >= highWaterMark) {
    return { allocated: true, code, ledger: ledger.concat(code) };
  }
  return { allocated: false, code };
}
