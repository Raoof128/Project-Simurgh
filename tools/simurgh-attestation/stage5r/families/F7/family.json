{
  "schema": "simurgh.vpf.family-file.v1",
  "note": "The frozen §3.1 record, plus the corpus binding it needs and cannot hold. The record's key set is exact by contract, so which inherited member each control MODELS, which file carries its bytes, and which language it is written in live under `binding` — outside the frozen shape rather than smuggled into it. A control is a hand-authored specimen of a role member, not a copy of one: 5R never writes to an inherited tree, so it cannot seed a defect into the member it models.",
  "record": {
    "probe_family_id": "F7",
    "attack_class": "R6",
    "target_security_role": "canonicalisation",
    "role_archetype": "A3",
    "inherited_5q_obligation_cells": 71,
    "vulnerable_control": {
      "premise_receipt": {
        "function_id": "5f:tools/simurgh-attestation/stage5f/core/digests.mjs:panelPlanDigest",
        "predicate": "the declared signal's construct is present in this control AND carries the defect the class names",
        "source_digest": "03af2f961dfa2505978ae1f25e14ef31b22df7863a48b5981eae1f6de593c229",
        "span_bytes": 271,
        "holds": true
      },
      "expected_detection": true,
      "expected_outcome": "two plans that differ in which keys are present canonicalise to identical bytes, so one digest covers both",
      "source_span_bytes": 271
    },
    "safe_control": {
      "expected_detection": false,
      "source_span_bytes": 270,
      "exercises_detector_signal_path": true
    },
    "orthogonal_failure_control": {
      "failure_mode": "throw",
      "expected_detection": false,
      "source_span_bytes": 354
    },
    "detector_signal": "canonicaliser_skips_a_key_instead_of_encoding_it",
    "forbidden_surrogate_signals": [
      "process exit code alone",
      "\"an exception was thrown\" alone",
      "\"stderr was non-empty\" alone",
      "\"the file did not parse\" alone",
      "\"the run took longer than a threshold\" alone",
      "\"any string matched a generic error regex\""
    ],
    "coverage_delta": []
  },
  "binding": {
    "models_function_id": "5f:tools/simurgh-attestation/stage5f/core/digests.mjs:panelPlanDigest",
    "category": "exported_function",
    "security_role": "canonicalisation",
    "language": "js",
    "symbol": "panelPlanDigest",
    "arity": 1,
    "controls": {
      "vulnerable": {
        "control_id": "F7-c1",
        "path": "vulnerable.control"
      },
      "safe": {
        "control_id": "F7-c2",
        "path": "safe.control"
      },
      "orthogonal": {
        "control_id": "F7-c3",
        "path": "orthogonal.control"
      }
    }
  }
}
