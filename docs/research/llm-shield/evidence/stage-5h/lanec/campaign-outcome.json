{
  "schema": "simurgh.vsd.campaign_outcome.v1",
  "status": "completed",
  "disclosure_present": true,
  "note": "Independent droplet team executed BOTH Lane-C roles on their own machines with their own Ed25519 keys (local + droplet 170.64.167.95, Node v26.5.0). REVIEW-HOST role (the load-bearing result): two unaffiliated hosts independently reran our committed Lane-A controlled recipe and reproduced its committed_output_digest sha256:9f10dc0d... BYTE-FOR-BYTE, then counter-signed R1 receipts with keys distinct from the Simurgh verifier. real-disclosure/ = our disclosed claim carrying the real droplet host receipt (host fp 8c12ec8c != verifier fp 6a5f0962), verifies raw 0 audit. HONEST BOUND: the PRODUCER role used the pack's placeholder refusal_rate 0.94 artefact, NOT a real eval; that half demonstrates cross-party producer signing only. The real independent reproduction is the review-host half."
}
